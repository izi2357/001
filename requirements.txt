pip install streamlit
pip install torch==2.0.0  # or the latest stable version compatible with your environment
pip install transformers==4.18.0  # or the latest stable version compatible with your environment
